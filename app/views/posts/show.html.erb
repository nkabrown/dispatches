<h1> Here are all of your posts </h1>
<p><%= "by " + @user.fname %></p>


<ul>
		<!-- Post.where(user_id:12) -->
		<% @posts.each do |post| %>
			<li><%= post.body %>
        <%= link_to "Edit", edit_post_path(@user) %></li>
		<%end%>
</ul>

<p> <%= link_to "Back to the Newsroom", users_path %> </p>
<p> <%= link_to "Sign-Out", session_path(@user), method: :delete %> </p>
