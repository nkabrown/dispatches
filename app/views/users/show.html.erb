<h1><%= @user.fname + "'s Desk" %></h1>

<ul>
		<% @posts.each do |post| %>
			<li><%= post.body %>
        <%= link_to "Edit", edit_post_path(@user) %></li>
		<%end%>
</ul>



<%= link_to "Back to the Newsroom", users_path %>
<%= link_to "Post a dispatch.", new_post_path %>
<%= link_to "Sign-Out", session_path(@user), method: :delete %>

